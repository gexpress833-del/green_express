# URL de l'API Laravel (sans /api à la fin). En dev, utiliser localhost comme le frontend pour que les cookies de session soient envoyés.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production : remplacer par l'URL du backend (ex: https://api.greenexpress.com)
