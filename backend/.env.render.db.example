# Coller ici l'Internal Database URL de ta base PostgreSQL Render (une seule ligne).
# Render → Base PostgreSQL → Connect → Internal Database URL
# Exemple (remplace par ta vraie URL) :
# postgres://user:password@dpg-xxxxx-a.oregon-postgres.render.com/dbname
